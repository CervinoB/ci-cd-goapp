name: ci-golang-workflow
on:
  pull_request:
    branches: [dev]
jobs:
  check-application:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v4
        with:
          go-version: ">=1.17.0"
      - run: go test
      - run: go run math.go
